import { Text } from 'react-native';
import styled from 'styled-components';
import { colors } from '../../constants/theme';

export const YourPlansText = styled(Text)`
  font-size: 18px;
  color: ${colors.primaryText};
`;

export const DayText = styled(Text)`
  font-size: 28px;
  color: ${colors.primaryText};
`;
